name: 'pt4_dbt_pascualdc'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  pt4_dbt_pascualdc:
    staging:
      +materialized: view
    marts:
      +materialized: table

seeds:
  pt4_dbt_pascualdc:
    +schema: SEEDS

snapshots:
  pt4_dbt_pascualdc:
    +schema: SNAPSHOTS